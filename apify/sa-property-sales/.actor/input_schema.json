{
    "title": "South Australia Property Data Scraper input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "searchQueries": {
            "title": "CKAN search queries",
            "type": "array",
            "description": "What to search for on data.sa.gov.au. Generic property queries by default — review results to see what South Australia publishes as open data.",
            "editor": "stringList",
            "prefill": [
                "property sales",
                "median house price",
                "land value"
            ]
        },
        "fileUrls": {
            "title": "Direct file URLs (CSV or Excel)",
            "type": "array",
            "description": "Direct links to CSV or XLSX/XLS files (e.g. a specific VG statistics spreadsheet). Downloaded and parsed as-is — the most reliable path for a specific published spreadsheet.",
            "editor": "stringList"
        },
        "suburbFilter": {
            "title": "Suburb filter",
            "type": "array",
            "description": "Keep only rows mentioning these suburbs/localities (case-insensitive substring match across all columns). Empty = keep everything.",
            "editor": "stringList",
            "prefill": [
                "Norwood"
            ]
        },
        "portalBaseUrl": {
            "title": "CKAN portal base URL",
            "type": "string",
            "description": "South Australia's open-data CKAN portal.",
            "editor": "textfield",
            "default": "https://data.sa.gov.au/data"
        },
        "maxDatasets": {
            "title": "Max datasets per query",
            "type": "integer",
            "default": 15,
            "minimum": 1,
            "maximum": 200
        },
        "maxRecordsPerResource": {
            "title": "Max rows per resource",
            "type": "integer",
            "default": 50000,
            "minimum": 1,
            "maximum": 1000000
        }
    },
    "required": []
}
