{
    "title": "Victorian Property Sales Scraper input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "searchQueries": {
            "title": "CKAN search queries",
            "type": "array",
            "description": "What to search for on data.vic.gov.au. Defaults target the Valuer-General property-sales statistics.",
            "editor": "stringList",
            "prefill": ["property sales statistics", "median house price"]
        },
        "csvUrls": {
            "title": "Direct CSV URLs",
            "type": "array",
            "description": "Direct links to CSV exports (e.g. a specific VG statistics spreadsheet saved as CSV). Downloaded and parsed as-is — the most reliable path when a dataset only ships XLSX.",
            "editor": "stringList"
        },
        "suburbFilter": {
            "title": "Suburb filter",
            "type": "array",
            "description": "Keep only rows mentioning these suburbs/localities (case-insensitive substring match across all columns). Empty = keep everything.",
            "editor": "stringList",
            "prefill": ["Richmond"]
        },
        "portalBaseUrl": {
            "title": "CKAN portal base URL",
            "type": "string",
            "description": "Victoria's open-data CKAN portal.",
            "editor": "textfield",
            "default": "https://discover.data.vic.gov.au"
        },
        "maxDatasets": {
            "title": "Max datasets per query",
            "type": "integer",
            "default": 15,
            "minimum": 1,
            "maximum": 200
        },
        "maxRecordsPerResource": {
            "title": "Max rows per resource",
            "type": "integer",
            "default": 50000,
            "minimum": 1,
            "maximum": 1000000
        }
    },
    "required": []
}
